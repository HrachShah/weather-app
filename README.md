# Weather App

A modern weather application built with HTML, CSS, and JavaScript.

## Features

- Current weather conditions display
- Multi-day forecast
- Location-based weather data
- Responsive design for all devices

## Usage

Simply open `weather app/Untitled-1.html` in a browser, or serve the `weather app` directory with any web server.

## Development

The application uses the OpenWeatherMap API for weather data. To use your own API key, update the API configuration in the JavaScript files.
